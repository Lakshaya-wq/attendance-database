<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css">
    <title>Class Attendance</title>
</head>
<body>
    <div>
        <div class="ribbon-wrapper">
          <div class="ribbon">DEV</div>
        </div>
      </div>
      <center>
        <h1 style="color: white;">Class Attendance for <%= standard.toUpperCase() %></h1>
        <% if (pcnt >= 70) { %>
            <h2 style="color: lightgreen;">Class Attendance <%= pcnt %>% 👍</h2>
        <% } else if (pcnt >= 40) { %>
            <h2 style="color: orange;">Class Attendance <%= pcnt %>%</h2>
        <% } else { %>
            <h2 style="color: red;">Class Attendance <%= pcnt %>% 👎</h2>
        <% } %>
    </center>
    <table class="table table-bordered table-light table-striped table-hover">
        <thead>
            <th>roll no</th>
            <th>date</th>
            <th>name</th>
            <th>present?</th>
        </thead>
        <tbody>
            <% for (let day of attendance) { %>
                <tr>
                    <td><%= day.roll_no %></td>
                    <td><%= day.date %></td>
                    <td><%= day.name %></td>
                    <td><%= (day.att === 'P') ? 'Present' : 'Absent' %></td>
                </tr>
            <% } %>
            <td colspan="4"><form method="POST" action="/export"><input type="text" hidden required name="standard" value="<%= standard %>"><input type="submit" value="download report" class="btn btn-primary"></form></td>
        </tbody>
    </table>
</body>
</html>